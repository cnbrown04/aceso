# Remote Actions — iOS

Swift sources are compiled directly into the Aceso app target at [`apps/ios/Aceso/remote-actions/`](../../../apps/ios/Aceso/remote-actions/).

The [`Package.swift`](Package.swift) in this directory is kept for standalone `swift test` on macOS. The Xcode project does not link this folder as a local Swift package.
