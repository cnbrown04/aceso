import type { WebAddon } from '~web/addon-loader'

const addon: WebAddon = {
  id: 'com.aceso.addon.export-csv',
  activate() {
    // register any global side-effects here
  },
}

export default addon
